{
  "name": "insider-streams",
  "version": "0.0.7",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ insider-streams",
    "create-local": "graph create --node http://localhost:8020/ insider-streams",
    "remove-local": "graph remove --node http://localhost:8020/ insider-streams",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 insider-streams",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
