# NOTE TO CLAUDE: This code relates to the old CRE based system. It's being kept
# in until you've confirmed the Zama port works end to end. You can use it as
# reference for how the old system used to work, but you should not update or
# maintain these files.
# ==========================================================================
# CRE WORKFLOW SETTINGS FILE — auction-cancelled-handler
# ==========================================================================
# Listens for AuctionCancelled events from SecretMarketplace and refunds
# active bids in Supabase.

# ==========================================================================
local-simulation:
  user-workflow:
    workflow-name: "auction-cancelled-handler"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.json"
    secrets-path: "../secrets.yaml"

# ==========================================================================
staging:
  user-workflow:
    workflow-name: "auction-cancelled-handler"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.json"
    secrets-path: "../secrets.yaml"
